"use strict";
//typescript tuples
// tuples are fixed length arrays and the element are known and need not to be same
// tuples are immutable
let items;
items = ['mango', 1];
items[0] = 'apple';
// items = [1, "mango"] // cant change the order
//never type
let empty;
// never type does not hold any value, you cannot assign a value to a variable with the never type.
// empty = 1;
// empty = 'hello';
// empty = true;
// empty = null;
// empty = undefined;
